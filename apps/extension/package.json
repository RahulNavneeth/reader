{
  "name": "@reader/extension",
  "private": true,
  "version": "0.1.0",
  "description": "Browser extension that captures pages into Reader.",
  "scripts": {
    "build": "vite build",
    "dev": "vite build --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.290",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
